function setup() {
    createCanvas(windowWidth, windowHeight);
}

function draw() {
    background(110);
    translate(windowWidth / 2, windowHeight / 2);
    strokeWeight(50);
    textSize(50);
    text('main', 0, 0);
}